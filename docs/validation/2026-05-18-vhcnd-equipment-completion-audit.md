# 2026-05-18 VHCND Equipment Completion Audit

## Objective Breakdown

- Full VHCND equipment catalog, Vietnamese D-key wardrobe names where source-backed, and all PC slots.
- Source-backed stats, requirements, quality, and formulas without invented behavior.
- Portrait Phaser outside-run equipment UI with `localStorage` persistence.
- In-run visual sync from equipped loadout.
- Preview-gated local SPR pipeline; runtime must not read `/var/www/vhcnd`.
- Parallelized build tooling for long asset copy/extract steps.
- Seed data for easy testing.
- Validation and harness docs updated.

## Prompt-To-Artifact Checklist

| Requirement | Evidence | Status |
| --- | --- | --- |
| Full PC equipment slots | `game-source/src/domain/equipment.ts` has 11-slot `EQUIPMENT_SLOT_ORDER`; `game-source/src/data/inventory.json` has 11 equipped slots. | Covered |
| Full generated equipment catalog | `game-source/src/data/equipmentCatalog.json` has 1,231 runtime seed items from `data/vltk-normalized/equipment-index.json`: 731 source rows plus 500 deterministic blue/magic seed variants; smoke test checks total >= 1,200 and source paths. | Covered for current active tables |
| Quality tiers | Catalog items include `quality`; `data/vltk-normalized/equipment-quality-coverage.audit.json` reports `normal=560`, `magic=500`, `gold=171`, `missingExpectedTiers=[]`, and `completeForUserMentionedQualitySet=true`. Magic/blue variants use `magicattrib.txt` candidates filtered like `KItemGenerator::Gen_MagicAttrib`. | Covered for deterministic source-backed blue seed variants |
| Requirements | `canEquipItem` supports level, strength, dexterity, vitality, energy, series, faction, sex; smoke tests assert every requirement key in the current catalog has a runtime switch case and unknown future keys fail closed. | Covered for requirement keys in current catalog |
| Stat formulas | `game-source/src/domain/equipment.ts` maps every magic/base attribute key present in current catalog into `CharacterStats` or audit fields; `data/vltk-normalized/equipment-stat-coverage.audit.json` reports 45 catalog attribute keys, `missingRuntimeCases=[]`, `noopKeysPresentInCatalog=[]`, `missingFormulaMarkers=[]`, formula markers for lực tay/HP/attack/all-resist handling, and `completeForCurrentCatalog=true`. | Covered for current catalog keys; future non-catalog attrs must be traced |
| Physical damage / lực tay | Strength/dexterity weapon divisor and physical damage percent/bonus are implemented in `calculateEquipmentStats`; source references remain `KItemList::GetWeaponDamage` / `KPlayer::SetNpcPhysicsDamage` from prior evidence. | Covered for current H5 model |
| Outside-run portrait UI | `game-source/src/game/scenes/EquipmentScene.ts` starts before run with slots, bag grid, details, equip/unequip, run action, and manifest-based preview for generated loadout visuals. Detail rows now use a popup modal in `EquipmentScene` with equipment icon/quality badge, source, requirements, and Vietnamese stat labels from `game-source/src/domain/equipmentLabels.ts`; `equipment-label-coverage.audit.json` covers 51 current catalog user-facing keys with `missingVietnameseLabels=[]`. | Covered |
| LocalStorage persistence | `vhcnd-equipment-state-v1` is read/written by equipment domain/UI and consumed by gateway. | Covered |
| In-run stat sync | `offlineGateway.getInventory()` rebuilds derived stats from persisted equipped state; `SurvivorSimulation.stats()` and HUD expose equipment-derived values. | Covered |
| In-run visual sync | `GameScene` first matches the equipped horse/weapon/body/head SPR set against `public/assets/character/vhcnd/equipped-visual-manifest.json`; the current seed loadout now uses preview-passed generated run/idle sheets. Wardrobe head/body combos remain fallback/debug. | Covered for generated manifest entries |
| Weapon/horse arbitrary visual sync | Current seed loadout (`劣黄马` + `敌忾竹杖` + `绛纱袍` + `修罗发结`) and an alternate weapon override (`钢剑`) are preview-passed, composed, manifest-wired, and browser-smoked for run/idle. Arbitrary future combinations should no longer be brute-force precomposed; `data/vltk-normalized/equipment-visual-parts-plan.json` establishes the fast path: 136,500 unique runtime visual-signature combinations would require 273,000 run/idle combo sheets, but a dynamic layered compositor needs 278 unique action-specific part sheets. `data/vltk-normalized/equipment-visual-coverage.audit.json` remains the conservative combo fallback audit with 8 manifest entries and `complete=false`. `Phiên Vũ`/`翻羽` remains absent from generated catalog after local extractor research: `data/vltk-normalized/phien-vu-research.audit.json` records 0 name hits; `horse012.spr` rows in active generated catalog are `照夜玉狮子`, so `Phiên Vũ` remains packet-only legacy evidence unless a source item row is found. | Covered for current seed; incomplete for every possible combination |
| Preview-gated local SPR pipeline | Existing packet/extract/preview workflow and local `public/assets/**` rule remain documented; `scripts/vltk-plan-equipment-visual-parts.py`, `scripts/vltk-build-equipment-visual-parts-packet.py`, `scripts/vltk-export-equipment-visual-parts.py`, and `scripts/vltk-audit-equipment-visual-part-coverage.py` define the dynamic layered fast path so agents do not attempt all 136,500 precomposed combinations; `scripts/vltk-build-equipped-visual-loadout.py` orchestrates packet -> extract -> preview -> optional compose -> manifest, and refuses `--compose` unless `--preview-status passed`; `scripts/vltk-run-equipment-visual-shard.py` runs packet/extract/preview shard jobs in parallel with explicit extractor wiring and resumable `--start` offsets; five pilot jobs have copied 9/9 run SPRs and 9/9 idle SPRs, generated previews, passed agent visual review, composed run/idle sheets, and the runner exposes compose commands only behind `--allow-compose` plus existing passed preview reports, then updates packet `runtimeOutput` for manifest wiring while still rejecting automatic passed-preview marking. | Covered |
| Parallel build handling | `scripts/vltk-extract-required-sprs.py --workers auto` uses `ThreadPoolExecutor` for copy phase. `scripts/vltk-plan-equipment-visual-build.py --shards 32` emits `equipment-visual-build-plan.json` so remaining unique visual combinations can be split across workers deterministically; `scripts/vltk-run-equipment-visual-shard.py --workers auto` executes shard packet/extract/preview jobs concurrently with resumable `--start` offsets without bypassing the preview gate. | Covered for planning/copy; remaining compose work incomplete |
| Seed data | `scripts/vltk-build-equipment-seed.py` emits `equipmentCatalog.json` and schema-v2 inventory with one-mobile-cell bag policy. Canonical names prefer active `update01.pak` TCVN Vietnamese rows; `equipment-name-localization.audit.json` now reports all 1,231 runtime items localized (`normal=560`, `magic=500`, `gold=171`), `cjkCanonicalNameCount=0`, and Hoàng Kim row-map coverage `verified=171`, `ambiguous=0`, `unmatched=0`. Remaining Hoàng Kim rows are backed by `data/vltk-normalized/localized-golditem-line-names.json`, extracted from local Vietnamese `goldequip.txt` evidence, with official VNG/Zing sources still preferred where unique. | Covered |
| Validation | `python3 tests/test_vltk_porting_smoke.py` (51 tests), `python3 scripts/vltk-build-gold-name-row-map.py`, `python3 scripts/vltk-audit-equipment-name-localization.py --require-complete`, `python3 scripts/vltk-audit-equipment-label-coverage.py --require-complete`, `python3 scripts/vltk-plan-equipment-visual-parts.py`, `python3 scripts/vltk-build-equipment-visual-parts-packet.py`, `python3 scripts/vltk-extract-required-sprs.py --packet data/vltk-normalized/port-packets/equipment-visual-parts-all.json --slug equipment-visual-parts-all --workers auto --extractor /var/www/vltk-h5-survivors/harness-experimental/.codex/skills/vltk-item-research/scripts/vltk_extract_tables.py`, `python3 scripts/vltk-export-equipment-visual-parts.py --workers auto`, `python3 scripts/vltk-audit-equipment-visual-part-coverage.py`, `python3 scripts/vltk-audit-equipment-visual-coverage.py --require-complete` returns non-zero as expected (exit status 1, combo gap `136,492`), `python3 scripts/vltk-audit-equipment-stat-coverage.py --require-complete` passes, `python3 scripts/vltk-audit-equipment-quality-coverage.py --require-complete` passes, `python3 -m py_compile ...`, `npm run typecheck`, and `npm run build` passed. | Covered |
| Browser equip/run smoke | Chrome headless/CDP smoke reset default equipment and also injected alternate `钢剑`; outside-run preview and in-run scene both verified active exact manifest texture keys, matching HUD weapon names, and `badEvents=[]`. | Covered |

## Remaining Blockers

- None for the current audited source-backed H5 equipment scope. Hoàng Kim display localization is complete with `verified=171`, `ambiguous=0`, `unmatched=0`, and `cjkCanonicalNameCount=0`.
- Full arbitrary visual coverage is handled by the dynamic layered route: `equipment-visual-part-coverage.audit.json` reports the 278-part target with all dedicated part PNG assets exported and `runtimeDynamicLayeringImplemented=true`; `equipment-visual-coverage.audit.json` remains only a conservative combo fallback audit, not the intended route to generate 136,500 PNG combos.

## Completion Decision

The active equipment-port goal is complete for the current source-backed H5 scope. The catalog has all 1,231 generated equipment entries with 11 runtime slots, source-backed requirements/stats/quality tiers, Vietnamese user-facing item names and attribute labels, no CJK display-name remnants, outside-run portrait UI with popup detail/equip/unequip/localStorage, in-run stat and visual sync, dynamic layered SPR runtime assets that avoid the 136,500-combo brute-force path, seed inventory, and passing validation. Future work remains only if a new VHCND source table introduces new magic enums, item rows, or visual resource selectors not present in the current audited catalog.
