interface:
  display_name: "VHCND SPR Porting"
  short_description: "Port and debug VHCND SPR visuals in H5."
  default_prompt: "Use $vhcnd-spr-porting to port, compose, and debug VHCND character/equipment SPR visuals, including per-direction layer issues and runtime cache/preset pitfalls."
