interface:
  display_name: "VHCND Map Porting"
  short_description: "Port VHCND maps into H5 safely."
  default_prompt: "Use $vhcnd-map-porting to port or fix a VHCND map with source-backed render parity."
