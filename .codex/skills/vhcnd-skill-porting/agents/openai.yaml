interface:
  display_name: "VHCND Skill Porting"
  short_description: "Port VHCND combat skills with evidence-backed parity."
  default_prompt: "Use $vhcnd-skill-porting to port a VHCND skill into the H5 runtime using active PAK/table/script/engine evidence only, then validate parity."
